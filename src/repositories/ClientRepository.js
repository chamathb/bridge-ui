/**
 * Created by chamathbogahawatta on 08/05/2019.
 */
import axios from "axios";
import {API_BASE_URL} from "../config/AppConfig";

const getClientById = (id) => {

};

const saveClient = ({name, bReg, address, vat, tin, contacts={}}, id) => {

};

const deleteClient = (id) => {

};

export default {
  getClientById,
  saveClient,
  deleteClient,
};